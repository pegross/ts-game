export default class Settings
{

    static fps: number = 30;
    static height: number = 200;
    static width: number = 300;

    static playerBaseSpeed: number = 3;
    static playerStartX: number = Settings.width / 2;
    static playerStartY: number = Settings.height / 2;
    static playerWidth: number = 20;
    static playerHeight: number = 20;

}