export enum DirectionX
{
    NONE = 0,
    LEFT = -1,
    RIGHT = 1,
}

export enum DirectionY
{
    NONE = 0,
    DOWN = 1,
    UP = -1
}

export enum Side
{
    TOP,
    RIGHT,
    BOTTOM,
    LEFT
}

export enum State
{
    IDLE,
    MOVING
}
